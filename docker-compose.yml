version: "3.2"
services:
  apache:
    build: './docker/apache/'
    networks:
      - frontend
      - backend
    ports:
      - "80:80"
      - "3306:3306"
    volumes:
      - ./BackendProject/:/var/www/html/
    environment:
      - MYSQL_ROOT_PASSWORD=etorn
networks:
  frontend:
  backend: